name: Setup Go Environment

on: [workflow_call]

jobs:
  setup:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-go@v4
        with:
          go-version: 1.14
      - run: go mod download
      - run: go mod verify